version: "2.2"
services:
  backend:
    container_name: "backend"
    image: ghcr.io/artidsun/int222-backend:latest
    volumes:
      - $PWD/public:/tmp
    ports:
      - "3000:80"
    restart: on-failure
