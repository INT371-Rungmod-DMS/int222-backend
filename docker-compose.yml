version: "2.2"
services:
  backend:
    container_name: "backend"
    build: .
    image: ghcr.io/artidsun/int222-backend
    volumes:
      - $PWD/public:/tmp
    ports:
      - "3000:80"
    restart: always
